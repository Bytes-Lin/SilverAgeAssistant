# 银龄助手开发进度清单

> 核对日期：2026-08-12。完成项以 Android/MiddleServer 已存在的可执行实现为准；只有界面占位或需求文档的功能不标记完成。本清单只统计开发内容，不评价安全审计和测试完成度。Agent 进度见 [`Agent 系统开发进度`](../03-agent-system/agent-development-status-2026-08-11.md)，非 Agent 功能核对见 [`非 Agent 功能开发进度`](../06-development/non-agent-progress-2026-08-12.md)。

## 1. 工程与双角色基础

- [x] 单 APK、老人模式与家属模式双导航；
- [x] Android 10（API 29）最低版本和 Jetpack Compose 工程；
- [x] 适老化主题、大字号、大按钮和固定老人首页；
- [x] 老人/家属基础资料表单及开发测试资料本地保存；
- [x] 家属注册、老人档案、一次性绑定码和手机号联合绑定；
- [x] 家属重新生成绑定码及老人设备重新绑定；
- [x] 家属 token、老人 device credential 的本地加密保存；
- [x] 应用重启后的角色、登录和绑定会话恢复；
- [x] 绑定撤销或凭证失效后返回身份选择页，并避免家属重复注册已有手机号；
- [x] 家属初次绑定后无需重启即可使用首页功能；
- [ ] 发布环境配置、CI 和正式签名流程。

## 2. 老人端文字聊天与模型组件

- [x] 对话气泡、打字输入和系统手写键盘入口；
- [x] OpenAI 兼容 `ChatModelProvider` 和 SSE 流式聊天；
- [x] llama.cpp/标准 OpenAI 方言映射；
- [x] system prompt、会话历史、长期记忆、Tools 和用户消息组装；
- [x] `temperature`、`top_p`、`top_k`、最大生成 Token 和上下文长度配置；
- [x] 日常聊天关闭思考并过滤 reasoning/`<think>` 内容；
- [x] 取消生成、网络错误映射和原请求重试；
- [x] 老人端 API Key 输入、Keystore + AES-GCM 本地保存/替换/删除；
- [x] 家属端非敏感模型配置及中台下发，老人端热读取本地 JSON；
- [x] 聊天页上下文占用圆形百分比；
- [x] 主聊天 Agent 的 8 轮窗口、旧 5 轮压缩/近 3 轮保留、Summary 独立压缩和 Token 预算管理；
- [x] 老人端全局语音开关、聊天/通知/新闻语音管线和播放仲裁设计；
- [x] 家属语音模型非敏感配置表单、Android 请求映射、本地 JSON 和老人补拉热更新；
- [x] 中台远程模型配置接口增加完整 `voice` 对象并通过 OpenAPI 验收；
- [x] 老人端全局语音开关 DataStore、角色隔离和设置 UI；
- [x] AudioRecord 内存 PCM 与录音状态；
- [x] Qwen ASR WebSocket Provider、识别结果和失败降级；
- [x] Qwen TTS WebSocket Provider、完整回答单次 PCM 播放；
- [x] 完整“录音 → ASR → MLLM → TTS → 播放”语音闭环代码接入；
- [x] ASR 连接中松手、页面/后台取消、麦克风释放和内部协程取消消息过滤；
- [x] 新闻前五条带本地日期时间的单次 TTS 和家属新通知首次入库播报；
- [ ] 系统 TTS 显式降级和多设备真机验收；
- [ ] 跨会话聊天历史恢复。

## 3. Agent Tool 与生活信息

- [x] 强类型 Tool schema、注册表和多轮 Tool Call 编排；
- [x] `get_current_time` 时间工具；
- [x] `get_weather` 天气工具；
- [x] `call_family_contact` 家属电话工具和老人确认拨号；
- [x] `report_family_situation` 向家属报告一般/紧急事件工具；
- [x] 工具号码不交给模型，由本地加密联系人快照解析；
- [ ] 通用确定性 Policy Engine；
- [ ] 可恢复的 Agent Task 状态机；
- [x] `list_today_reminders` 只读工具、今日未确认事项确定性路由和老人可读结果整理；
- [ ] 统一提醒 Tool 的 `local_deadline_time` 与结果整理器 `local_time` 字段并补测试；
- [ ] 按实际 Registry 收紧主 Agent system prompt，移除尚未注册的提醒修改、新闻查询等能力宣称；
- [ ] 创建/修改提醒工具；
- [x] 百度热搜新闻 Provider、前 15 条文字列表和 2 小时暂存；
- [x] 新闻页面前 5 条单次 TTS 播报；
- [ ] 新闻摘要 Agent Tool；
- [ ] RAG 文档导入、分块、索引、检索和引用。

## 4. 天气与首页信息

- [x] Android 粗略定位和定位权限处理；
- [x] Android/网络后备反向地理编码得到真实城市名称；
- [x] Open-Meteo 当前天气和未来三天预报；
- [x] 首页天气卡片、恶劣天气提示和独立最近提醒卡片；
- [x] 首页与聊天 Tool 共享缓存；
- [x] 从天气响应保存位置 IANA 时区，供用量统计使用；
- [ ] 天气 Provider 可配置切换和更完整的离线降级。

## 5. 提醒与本地数据

- [x] Room `Reminder` 基础表和 Repository；
- [x] 家属通知/一次性提醒在老人端幂等落库；
- [x] 老人端新提醒首次落库后发布系统通知，重复补拉不重复弹出；
- [x] 今日提醒按时间排序，已完成置底，稍后提醒独立样式；
- [x] 老人可将提醒标记为完成或稍后；
- [ ] ReminderOccurrence、重复规则和执行历史；
- [ ] 老人端本地创建、编辑和删除提醒；
- [x] 远程提醒按截止时间使用 WorkManager 触发，并在未完成时每 1 小时重复本地通知；
- [x] 未完成提醒不因未来截止、跨时区换算或逾期而从列表隐藏，完成项仅在完成当天保留；
- [ ] “稍后提醒”真正重新调度，而不只是状态标记；
- [x] 新家属通知首次入库 TTS 播报；
- [ ] 本地到时提醒 TTS 播报；
- [ ] 语音创建提醒；
- [x] 应用重启扫描 Room 恢复未完成任务，WorkManager 跨进程退出和设备重启保留周期任务；
- [ ] 家属创建提醒时使用老人设备时区；当前使用家属设备系统时区，跨时区设备可能产生截止时刻偏移；

## 6. Agent 记忆

- [x] 应用私有目录 `files/agent/MEMORY.md` 长期记忆基础；
- [x] 绑定后写入老人称呼和家属称呼/关系；
- [x] 联系人同步后更新家属记忆区段；
- [x] 应用冷启动完整读取一次长期记忆，并在本次进程中复用不可变快照；
- [x] 预留经确认事实的追加区；
- [ ] MemoryItem 结构化数据模型、来源、置信度、敏感级别和有效期；
- [x] 滑动窗口压缩提取明确稳定事实，经用户原话 evidence 校验和写入限额后落盘；
- [ ] 短期任务记忆与跨会话恢复；
- [x] `MEMORY.md` 启动时完整读取，会话中新增事实在下一次冷启动后生效；
- [x] 主聊天 Agent 上下文预算、8 轮窗口、5/3 滚动压缩和 Summary 独立压缩；
- [ ] 旧对话事实/推测/待确认信息分层（重要事件摘要已实现）；
- [ ] 老人或家属查看、修改、删除记忆的管理页面。

## 7. 家庭协同与中台

- [x] FastAPI + SQLite 单进程中台基础；
- [x] 家属注册、token 刷新、老人档案、绑定码和设备凭证；
- [x] 家属资料同步到老人端并形成加密离线联系人快照；
- [x] 家属发送通知和创建一次性提醒；
- [x] 老人 REST 补拉、Room 幂等保存和 ACK；
- [x] 老人端完成确认 Room 队列、幂等重试和家属提醒记录 Android 页面；
- [x] 在线 WebSocket 数据可用提示；
- [x] 老人端 `COMMAND_AVAILABLE`、WebSocket 连接/重连和应用回前台自动 REST 补拉；
- [x] 家属端提醒完成与安全事件 WebSocket 提示、重连和回前台自动刷新；
- [x] 家属今日状态、紧急事件列表和紧急弹窗 ACK；
- [x] 家属紧急事件逐条清除，中台同步 `resolved_at` 后从活动列表移除；
- [x] 家属首页最近紧急事件摘要和新紧急事件 Android 系统通知；
- [x] 家属提醒记录逐条清除 UI、请求模型和接口映射；
- [ ] 中台家属账号级提醒记录归档接口和查询过滤；
- [x] 家属设置状态检测开关与间隔并实时下发；
- [x] 非敏感模型配置跨端同步；
- [x] 模型用量批量上报、手动即时刷新和家属查询；
- [x] 家属日用量汇总及月度每日 Token/ASR/TTS 柱状图；
- [x] 中台支持事件处理状态、活动紧急事件查询和 GUI 点单协助事件类型；
- [x] Android 接收并展示 `GUI_ORDER_ASSISTANCE_REQUIRED`，第二次完整失败可通知家属；
- [ ] Android 各类事件完整适配 `resolved_at`；
- [ ] 家属端今日状态普通事件的完成/清除和自然日清理规则；
- [ ] 通用事件 Outbox 和后台自动重试队列；
- [ ] 统一 `last_server_sequence` 断线补偿，而不是按功能分别补拉；
- [ ] 多老人档案切换和管理 UI；
- [ ] 家属远程管理紧急联系人和联系优先级；
- [ ] 通用审批事件和超时处理。

## 8. 老人异常状态监控

- [x] 与聊天 Agent 分离的专用状态监控 Agent；
- [x] `specialUse` 前台服务周期调度；
- [x] 家属运行中热更新 1/5/10/15/30/60 分钟间隔或关闭；
- [x] mock 图片源和未来 `NetworkCameraImageSource` 接口；
- [x] 独立 OpenAI 兼容 MLLM 图像分析和严格 JSON 结果；
- [x] 六小时本地状态窗口和连续异常计数；
- [x] 连续两次异常时每窗口只上报一次，连续三次时只发一次短信；
- [x] 异常事件图像上传、家属缩略图和点击查看原图；
- [x] 家属“今日状态/紧急事件”分类、按时间排序和弹窗确认；
- [ ] 真实局域网摄像头发现、鉴权和抓取实现；
- [ ] 摄像头断线、图像过期和多摄像头选择；
- [ ] 可配置的监控时段，而不是全天固定运行；
- [ ] 正式设备上的后台持续运行与系统限制处理。

## 9. GUI Agent、网购与外卖

- [x] GUI Agent 异步 Tool 门面和进程内单任务调度核心；
- [x] 最小 GUI Todo Room 表，只保存任务内容、状态、完整失败次数和协助事件 ID；
- [x] 第一次完整 GuiRun 失败后告知并自动再试，第二次完整失败调用家属协助出口；
- [x] 暂停/继续/取消控制门和离开目标 App、人工干预、语音输入暂停原因模型；
- [x] 主聊天/GUI Agent 共享时间 Tool 实例，其他 Tool 暂不共享；
- [x] GUI Planner 单步 Action 协议及 `use_tool` 白名单，当前仅允许共享 `get_current_time`；
- [x] 共享 ASR/TTS Provider、GUI 独立用量 feature、单步播报和覆盖层按住说话；
- [x] 截图像素预算、等比 resize 规划、帧 ID 和模型坐标反向映射核心；
- [x] 主聊天 `gui_agent` Tool 正式注册，美团、微信、淘宝包名启动及前台验证链路；
- [x] 银龄助手前台及跨 App 覆盖层的暂停/继续/返回任务、按住说话和二次确认取消；
- [x] 用户授权后的 Accessibility 系统控制覆盖层和人工操作/离开目标 App 暂停映射；
- [x] GUI Agent 超时或两次完整失败后停止自动操作，并幂等创建家属紧急协助事件；
- [x] `GUI_ORDER_ASSISTANCE_REQUIRED` 家属协助出口和老人端确定性结果反馈；
- [x] 真实自动动作的 Accessibility 事件抑制及目标 App 内页面切换防抖；
- [x] AccessibilityService 最小原型、启用引导和跨 App 系统覆盖层；
- [x] 屏幕节点树观察、敏感字段过滤以及支付/密码/验证码页面拒绝；
- [x] API 30+ Accessibility 整屏截图、目标窗口裁剪、截图 resize 和真机执行接口；
- [x] 顶部悬浮条排除、截图内存清理和敏感页面拒绝上传；
- [ ] API 34+ 指定窗口截图优化和 API 29 MediaProjection 授权兼容路径；
- [x] 当前帧页面观察、点击/输入/滚动执行器、旧帧拒绝和动作后截图校验；
- [ ] 节点树不可用时的 OCR/VLM 观察接口；
- [ ] GUI Agent 可恢复任务状态机；
- [ ] 模拟外卖/网购 App 和稳定测试流程；
- [ ] 商品/餐品搜索、筛选和候选推荐；
- [ ] 地址、规格、数量、配送时间和价格摘要；
- [ ] 页面或价格变化后强制重新确认；
- [x] 提交订单前老人确认门；
- [ ] 金额阈值和家属审批链路；
- [ ] 一个真实 App 的有限白名单流程；
- [x] 支付页面停止自动化，由老人自行完成系统认证。
- [x] `guiDebugEnabled` 构建级全局开关；Debug 默认关闭调试 UI/追踪/坐标实验，Release 强制关闭；

## 10. 其他尚未落地的产品功能

- [ ] SOS 倒计时、本地拨号/短信/定位和中台协同；
- [x] 百度热搜新闻文字获取、双版页面解析和暂存；
- [x] 新闻页面前 5 条单次语音播报；
- [ ] 新闻摘要 Tool；
- [x] 移除老人首页“生活助手”和“紧急求助”手动入口，原占位页面不计为已交付功能；
- [ ] 网购外卖之外的受控手机辅助；
- [ ] 设置总页、数据导出和数据删除入口。

## 建议的下一开发顺序

1. 完成记忆治理基础：结构化 MemoryItem、写入确认、检索和管理页；
2. 基于现有 Accessibility、节点/坐标定位和重复步骤保护，建立可重复的真实 App 有限流程；
3. 建立通用 Policy Engine，并补齐订单摘要、金额阈值和家属审批；
4. 补齐 API 29 截图、OCR/VLM 后备观察和可恢复 GUI 任务状态；
5. 扩展本地提醒创建/修改、新闻摘要等低风险 Tools；
6. 完成语音多设备、API 29、系统 TTS 降级和 Agent 安全回归测试。
