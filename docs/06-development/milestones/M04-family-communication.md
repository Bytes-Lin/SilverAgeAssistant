# MILESTONE 04：家属通信与 FastAPI

当前状态：进行中。Android 已接入中台家属注册、老人档案、绑定码和老人设备绑定链路；家属端与老人端已在本地模拟器通过 `58.199.163.98:8765` 完成在线联通验证。

## Android 前置 UI（已完成）

- [x] 双角色初始化选择；
- [x] 老人端基础资料、家属手机号与可选绑定码表单；
- [x] 家属端双手机号资料、关系和紧急联系人表单；
- [x] 家属离线首页与“最后同步”状态；
- [x] 绑定字段校验和“待中台确认”状态；
- [x] 中台注册、绑定码和手机号联合校验交付需求；
- [x] Debug 测试用基础资料 DataStore 持久化（排除绑定码和凭证）；
- [x] 生产角色默认值和正式初始化状态持久化；
- [x] 绑定请求/响应模型与可替换 Repository；
- [x] Debug 中台地址配置和仅 Debug 明文 HTTP 放行；
- [x] Android Keystore + AES-GCM 加密保存家属令牌和 device credential；
- [x] 模拟器在线完成家属注册、生成绑定码和老人绑定全链路；
- [x] 重启会话恢复、旧加密凭证迁移与离线首页降级；
- [x] 家属 access token 刷新后重试绑定查询；
- [x] Android 家属通知和一次性提醒表单及中台 Repository；
- [x] Android 老人端命令补拉、Room 幂等落库和 ACK 重试；
- [x] Android 老人端提醒完成离线队列、幂等回传和家属提醒记录页面；
- [x] Android 老人端命令提示、WebSocket 重连和应用回前台自动 REST 补拉；
- [x] Android 家属端完成状态/安全事件提示、重连和回前台自动刷新；
- [x] 老人端新提醒系统通知与家属端紧急事件系统通知；
- [x] Android 家属提醒记录逐条清除 UI 与 archive 请求映射；
- [x] 中台通知/提醒接口与验收需求文档；
- [x] 老人端今日提醒按事件时间排序、已完成置底和稍后提醒独立配色；
- [x] Android 真实家属联系人客户端、加密离线快照和系统拨号入口；
- [x] 老人设备同步家属资料的中台接口与验收需求文档；
- [x] Android 家属端重新生成绑定码入口和现有接口接入；
- [x] 老人设备恢复绑定的中台需求与兼容接口文档；
- [x] Android 家属非敏感模型配置表单、老人端补拉与本地原子保存；
- [x] Android 聚合模型用量账本、小时上报任务和家属汇总页面；
- [x] Android 状态检测间隔配置、安全事件提交、家属今日状态与紧急事件 ACK；
- [x] 家属紧急事件逐条清除及中台处理状态同步；

中台首版约束：单进程 FastAPI + SQLite，不引入 PostgreSQL、Redis、`.env.example`、`infra/` 或数据库 Docker 依赖。

- [x] FastAPI 工程与迁移；
- [x] 家属直接注册（轻量联调版不接短信验证）；
- [x] 老人档案和绑定码；
- [x] 设备凭证；
- [x] 绑定查询、撤销与主体权限过滤；
- [x] 绑定幂等、失败限流、并发消费和脱敏审计测试；
- [x] 状态监控和老人主动报告的结构化事件 API 与幂等；
- [x] 提醒完成结果回传和家属提醒记录查询 API；
- [ ] 家属账号级提醒记录归档 API 与查询过滤；
- [ ] SOS、审批等通用事件 API；
- [ ] Android 通用事件 outbox（通知/提醒当前复用稳定请求 ID，尚未建立后台自动重发队列）；
- [x] 通知/提醒 `COMMAND_AVAILABLE` WebSocket 提示和 `STORED` ACK；
- [x] 通知/提醒 REST 断线补拉；
- [x] 家属今日状态和紧急事件页；
- [x] 中台远程通知/提醒指令接口；
- [x] 中台 `GET /devices/me/family-contacts` 家属资料快照接口；
- [x] 中台已有绑定场景的设备凭据原子轮换与恢复绑定；
- [x] 中台家属远程模型配置、revision 乐观锁与设备 REST 补拉；
- [x] 中台模型用量批量上报、幂等聚合与家属时间段汇总；
- [x] 中台用量位置时区保存、老人当地自然月每日分桶与零日期补齐；
- [x] 中台家属手动刷新用量接口、在线设备 WebSocket 提示与离线结果；
- [x] 中台状态检测配置 revision/幂等、设备补拉和 WebSocket 提示；
- [x] 中台状态检测 `enabled` 启停持久化、旧配置默认开启迁移和热更新提示；
- [x] 中台结构化安全事件持久化、当地今日查询、家属 WebSocket 提示和首次 ACK；
- [x] 中台老人主动身体不适/家属请求上报，以及按事件类型强制严重级别；
- [x] 中台异常事件证据图像上传、私有存储、去 EXIF 缩略图、授权读取、限期清理和在线提示；
- [x] 同一 FastAPI 应用内的开发期本机管理页面：默认展示有效绑定对、脱敏筛选、称呼/关系更新和带审计软撤销；
- [x] 管理启动器支持自定义监听 IP/端口，默认不打开浏览器；外部设备可访问业务接口但不能访问 `/admin`；
- [ ] 双设备端到端测试。
