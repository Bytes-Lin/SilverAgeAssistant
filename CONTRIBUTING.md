# Contributing

1. 先阅读根目录 `AGENTS.md` 和任务相关文档。
2. Android 改动放在 `AndroidAgent/`；FastAPI 改动放在 `MiddleServer/`。
3. 一个 PR 只解决一个明确问题。
4. 不提交秘密、构建产物或真实用户数据。
5. 新功能必须包含失败路径、测试和文档更新。
6. UI PR 提供截图或录屏；API PR 更新 OpenAPI/API 草案。
7. 安全、SOS、支付和隐私相关改动需要二次审查。
8. 提交信息使用 `type(scope): summary`，例如 `feat(android-chat): add recording state machine`。
